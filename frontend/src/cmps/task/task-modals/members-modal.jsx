export function MembersModal({ getMembers, onSelectMember }) {

    // function getMembers() {
    //     let members = board.members.filter(member => task.memberIds.indexOf(member._id) !== -1)
    //     return members
    // }
    return (
        <section className='members-modal-container'>
            <div className="members-modal-header">
                <input type="text" />
            </div>

        </section>
    )
}